name: meshroom
channels:
  - pytorch-nightly
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - pytorch 
  - torchvision 
  - cudatoolkit=11
  - pip  
  - pip:
      - opencv-python
      - numpy
      - plyfile
      - pillow
      - tensorboard